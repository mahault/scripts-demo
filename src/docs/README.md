# Architecture README (TODO)

More detailed exposition of the architecture here (after we do some tests)

This repository is scaffolded into:
- `architecture_core/` : robot-agnostic core you ship
- `plugin_templates/` : examples for implementers

Next steps:
- wire perception
- add real ToM planner adapter
- implement first real robot plugin (Webots TIAGo)
