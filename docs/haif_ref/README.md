# Hierarchical Active Inference for Continuous Control

This package provides a hierarchical active inference agent for robot continuous control. Several robots are simulated.

## Installation with `pyenv`

You can create a virtual environment with `pyenv` and install dependencies there:

```
pyenv virtualenv 3.11 haif
pyenv activate haif
pip install -e .
python examples/lerobot.py
```