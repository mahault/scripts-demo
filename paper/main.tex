\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{natbib}
\usepackage[margin=2.5cm]{geometry}
\usepackage{xcolor}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric}

\newcommand{\vfe}{\mathcal{F}}
\newcommand{\efe}{\mathcal{G}}
\newcommand{\A}{\mathbf{A}}
\newcommand{\B}{\mathbf{B}}
\newcommand{\CC}{\mathbf{C}}
\newcommand{\D}{\mathbf{D}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\KL}{\mathrm{D}_{\mathrm{KL}}}
\newcommand{\HH}{\mathrm{H}}

\title{Environment Shapes Cognition: How Material Structure Parameterizes Active Inference for Social Robot Behavior}

\author{%
  Mahault Albarracin\textsuperscript{1,2}
  \\[6pt]
  \textsuperscript{1}Department of Computer Science, Universit\'e du Qu\'ebec \`a Montr\'eal \\
  \textsuperscript{2}VERSES AI Research Lab
}

\date{\today}

\begin{document}

\maketitle

\begin{abstract}
How does the material environment shape a social robot's behavioral repertoire?  We present computational experiments testing this question through the lens of active inference and compositional script assembly.  Our system implements a discrete-state generative model where environmental context parameterizes both which behavioral fragments are activated ($\D$-matrix gating) and how composed policies are evaluated ($\CC$-matrix preferences).  Through three experiments---a material cue factorial (48 conditions), a fragment scaling study (189 conditions), and a context transfer test (3 conditions)---we demonstrate that environment shapes cognition at every level.  First, material cues interact with context to determine which fragments pass the $\D$-matrix gating threshold, producing different primitive sets across environments: a corridor robot composes 6 primitives focused on yielding and courtesy, while a reception robot composes 9 primitives including queuing and staff engagement.  Second, the compositional pipeline degrades gracefully under model sparsity, with both structural and evaluative metrics now diverging across contexts (Spearman $\rho > 0.74$ for all contexts).  Third, identical knowledge bases yield qualitatively different behaviors under different contexts, with Jaccard distances up to 0.50 between primitive sets and large structural effect sizes (Cohen's $d = 1.02$ for policy length between reception and corridor).  These results ground the claim from embedded normativity: the environment does not merely bias how the robot evaluates its behavior---it determines which behaviors are even possible.
\end{abstract}

\noindent\textbf{Keywords:} active inference, social robotics, compositional assembly, generative model, material semiotics, variational free energy, expected free energy


% ================================================================
\section{Introduction}
\label{sec:introduction}
% ================================================================

The relationship between environment and cognition has long been a central concern in cognitive science, social theory, and robotics.  \citet{lefebvre1991production} analyzed how spatial organization produces social relations.  \citet{latour1992missing} showed how mundane artifacts carry behavioral scripts.  \citet{akrich1992description} developed the concept of technical objects as inscriptions of designer intent.  Across these traditions, a consistent theme emerges: the material environment is not a neutral backdrop to social behavior but an active participant in shaping it.

For social robots that must navigate human environments, this insight has practical consequences.  A robot entering a hospital waiting room, a reception area, or a corridor encounters different material structures (stanchions, signs, desks, spatial configurations) that specify different behavioral expectations.  The question is not merely \emph{what} the robot should do in each context, but \emph{how} the environment itself can parameterize the robot's decision-making process to produce contextually appropriate behavior.

Active inference \citep{friston2017active,dacosta2020active,parr2022active} provides a principled framework for addressing this question.  Under active inference, an agent maintains a generative model of its environment and selects actions by minimizing expected free energy, a quantity that balances pragmatic value (achieving preferred outcomes) with epistemic value (resolving uncertainty).  The generative model is parameterized by matrices that specify observation likelihoods ($\A$), state transitions ($\B$), outcome preferences ($\CC$), and prior beliefs about initial states ($\D$).

The variational approach to scripts \citep{albarracin2021variational} extends this framework to sequential social behavior.  Scripts, understood as culturally shared expectations about event sequences, are formalized as multi-step generative models.  Weak scripts are unordered semantic clusters whose internal topology reshapes depending on context, while strong scripts are crystallized temporal sequences.  The transition from weak to strong is mediated by precision accumulation through experience.

Building on these foundations, we present a compositional assembly pipeline that constructs behavioral sequences from fragment patterns, which are partial generative models encoding knowledge about specific aspects of social encounters.  The pipeline implements belief propagation on a factor graph to retrieve relevant fragments, merges their primitive libraries into a unified transition model, extracts a causal backbone, and selects the composed policy via softmax over negative expected free energy.

Our central claim is that the \emph{environment shapes cognition} by gating which generative sub-models are activated and by parameterizing how composed policies are evaluated.  The $\D$-matrix prior, which encodes context-specific fragment affinities, determines which behavioral fragments are contextually relevant: fragments whose affinity falls below a gating threshold are suppressed, just as a corridor environment suppresses queuing scripts even when the agent knows how to queue.  The $\CC$-matrix preferences then reshape the evaluation landscape for the remaining fragments.  The result is that a corridor robot does not merely evaluate its behavior differently from a reception robot---it produces qualitatively different behavior, with a Jaccard distance of 0.50 between their primitive sets.

We investigate three research questions.  First (RQ1), do material cues interact with context to shape the generative model's active fragment set and thereby the composed policy?  Second (RQ2), does compositional behavior degrade gracefully as the generative model becomes sparser, and does context modulate both structural and evaluative properties?  Third (RQ3), does the same knowledge base yield qualitatively different behaviors under different environmental contexts?


% ================================================================
\section{Background}
\label{sec:background}
% ================================================================

\subsection{Active Inference on Discrete State Spaces}

Active inference \citep{friston2017active,dacosta2020active,parr2022active} casts perception, learning, and action selection as variational inference under a single generative model.  For discrete state spaces, the generative model takes the form
\begin{equation}
  P(\tilde{o}, \tilde{s}, \pi) = P(s_1) \prod_{\tau=2}^{T} P(s_\tau | s_{\tau-1}, \pi) \prod_{\tau=1}^{T} P(o_\tau | s_\tau)
  \label{eq:generative-model}
\end{equation}
where $\tilde{o} = (o_1, \ldots, o_T)$ and $\tilde{s} = (s_1, \ldots, s_T)$ are observation and hidden state sequences, and $\pi$ denotes a policy (sequence of actions).  The model is parameterized by four matrices.  The $\A$-matrix, $P(o_\tau | s_\tau)$, is the observation likelihood specifying which observations are expected in each hidden state.  The $\B$-matrix, $P(s_\tau | s_{\tau-1}, \pi)$, is the transition model specifying how states evolve under each policy.  The $\CC$-matrix, $\ln P(o)$, encodes log-preferences over observations, defining what the agent considers desirable outcomes.  The $\D$-matrix, $P(s_1)$, is the prior over the initial hidden state.

The agent maintains an approximate posterior $Q(s)$ over hidden states and evaluates policies by their expected free energy \citep{friston2017active,parr2019generalised}:
\begin{equation}
  \efe(\pi) = \sum_{\tau} \efe(\pi, \tau)
  \label{eq:efe-sum}
\end{equation}
where each time-step contribution decomposes into risk and ambiguity:
\begin{align}
  \efe(\pi, \tau) &= \underbrace{\E_{Q(o_\tau|\pi)}\left[-\ln P(o_\tau | \CC)\right]}_{\text{Risk (pragmatic value)}} + \underbrace{\E_{Q(s_\tau|\pi)}\left[\HH[P(o_\tau | s_\tau)]\right]}_{\text{Ambiguity (epistemic value)}}
  \label{eq:efe-decomposition}
\end{align}
Risk measures the expected divergence between predicted and preferred outcomes, while ambiguity measures the expected entropy of the observation model, quantifying how uncertain the mapping from hidden states to observations is.  Policies are selected via softmax over negative expected free energy:
\begin{equation}
  P(\pi) = \sigma(-\gamma \cdot \efe(\pi))
  \label{eq:policy-selection}
\end{equation}
where $\gamma$ is a precision parameter (inverse temperature) that controls the trade-off between exploration and exploitation \citep{friston2015active}.

The quality of the agent's beliefs is measured by variational free energy:
\begin{equation}
  \vfe = \E_{Q(s)}[\ln Q(s) - \ln P(o, s)] = \KL[Q(s) \| P(s|o)] - \ln P(o)
  \label{eq:vfe}
\end{equation}
which provides an upper bound on surprisal $-\ln P(o)$.  Minimizing $\vfe$ with respect to $Q$ performs approximate Bayesian inference, while minimizing with respect to the generative model's parameters performs learning.

\subsection{Variational Scripts}

\citet{albarracin2021variational} formalize social scripts as multi-step generative models within the active inference framework.  A script is a structured expectation about how events unfold in a social encounter, what \citet{schank1977scripts} called ``scripts, plans, goals, and understanding.''

Under this variational formulation, the $\A$-matrix encodes event recognition, specifying which observable features (material cues, social signals, spatial configurations) are expected at each stage of the script.  The $\B$-matrix encodes sequential transitions, capturing the expected ordering of events as parameterized by context.  Weak scripts are unordered semantic clusters with context-dependent topology, meaning that the same concepts (queuing, approaching, waiting) can be arranged differently depending on environment.  Strong scripts are pragmatically sequenced: crystallized temporal orderings that have been validated through repeated experience.  Script violation manifests as prediction error, formalized as a KL divergence between expected and observed event sequences.

The transition from weak to strong scripts is a structural transformation mediated by $\B$-matrix crystallization, not merely a precision increase.  As the agent accumulates experience in a context, the initially flexible topology of the weak script becomes increasingly constrained, converging on a specific sequential ordering.

\subsection{Embedded Normativity and Cultural Affordances}

\citet{guenincarlut2023embedded} develop the concept of embedded normativity, the idea that social norms are externalized into material environments.  This creates a circular causality in which environments encode preferences through material structure, agents conform to those preferences through free energy minimization, and the resulting behavioral regularities stabilize the environmental structure.

Material cues function as deontic affordances: they do not merely \emph{suggest} behavior but \emph{specify} it.  Stanchions do not suggest queuing; they make queuing the free-energy-minimizing policy.  This perspective aligns with \citet{latour1992missing}, who argued that the ``missing masses'' of sociology are the material artifacts that carry behavioral programs.

\citet{albarracin2022cultural} extend this analysis to cultural affordances, understood as possibilities for action that are stabilized by normative uptake.  Scripts are distributed across material and social substrates, partially encoded in the agent's generative model and partially inscribed in the environment.  The composed behavior emerges from the interaction between internal model and external structure.

\subsection{Compositional Memory and Dual-Loop Architecture}

\citet{nair2026thinking} propose a dual-loop architecture for active inference agents consisting of a fast perception-action loop operating at the time scale of individual actions and a slow compositional memory loop that assembles new behavioral sequences from stored fragments.  Fragment retrieval operates via diffusion on a factor graph, an iterative belief propagation process that activates relevant fragments based on contextual similarity and shared primitive structure.

Our compositional assembly pipeline implements this dual-loop architecture.  The fragment graph constitutes the slow compositional memory, and backbone extraction from the merged $\B$-matrix constitutes the crystallization step that converts flexible cluster structure into a specific temporal ordering.  Free energy serves as the unifying objective across both loops.


% ================================================================
\section{System Architecture}
\label{sec:architecture}
% ================================================================

\subsection{Generative Model Structure}

Our system implements a discrete-state generative model (\cref{eq:generative-model}) whose state and observation spaces are defined as follows.

The hidden state space is $s \in S = \{\texttt{open\_area}, \texttt{scene\_assessed}, \texttt{in\_queue}, \texttt{ready\_for\_service}, \texttt{at\_counter}, \texttt{interaction}\}$.  These represent the progressive phases of a social service encounter, from initial arrival to completed interaction.  Each state has an associated phase number $\phi(s) \in \{0, 1, 2, 3, 4, 5\}$ encoding its position in the encounter progression.

The observation space $o \in O$ consists of material cues, the perceivable environmental features that characterize each context, including stanchions (queue barriers), desk presence, directional signs, waiting area markers, and social density indicators.

The $\A$-matrix $P(o | s)$ specifies which material cues are observable in each situation.  In our implementation, this is implicitly encoded in the precondition-postcondition structure of primitives.  A primitive's precondition situations determine which states it can be executed from, and thus which observations trigger it, while its postcondition situation determines the resulting state.

The $\B$-matrix $P(s' | s, \pi)$ is constructed by the composition pipeline's topology builder as a context-dependent directed graph over primitives.  Specifically, for primitives $A$ and $B$ in context $C$, the unnormalized transition weight is
\begin{equation}
  \B(A \to B \mid C) = w_{\text{base}} \cdot r(A, C) \cdot r(B, C)
  \label{eq:b-matrix}
\end{equation}
where the base weight $w_{\text{base}}$ equals 1.0 when the postcondition of $A$ appears among the preconditions of $B$, and 0.1 otherwise.  The context relevance function $r(X, C)$ equals 1.0 when context $C$ appears among the preconditions of primitive $X$, and 0.2 otherwise.  After computing all pairwise weights for a given context, the values are normalized to the range $[0, 1]$ by dividing by the maximum weight.

The $\CC$-matrix $\ln P(o)$ encodes preferred observations per context.  Different contexts define different $\CC$-vectors, determining which composed policies minimize expected free energy.

The $\D$-matrix $P(s_1) = \texttt{situation\_affinity}$ is parameterized per context.  Each fragment pattern carries a dictionary of affinities mapping contexts to prior weights.  Crucially, the $\D$-matrix serves a dual role: it both weights fragment relevance during composition and \emph{gates} which fragments are activated at all.

\subsection{$\D$-Matrix Gating}

A key architectural feature is context-dependent fragment gating.  Each fragment's $\D$-matrix affinity for the current context is compared against a threshold $\theta$.  Fragments whose affinity falls below $\theta$ are excluded from the composition entirely:
\begin{equation}
  \mathcal{F}_{\text{active}}(c) = \{ f \in \mathcal{F} : a_f(c) \geq \theta \}
  \label{eq:d-gating}
\end{equation}
where $\mathcal{F}$ is the set of available fragments, $a_f(c)$ is fragment $f$'s affinity for context $c$, and $\theta = 0.4$ is the gating threshold.

This mechanism models how material environments suppress contextually irrelevant scripts.  A corridor environment has no service counter, so the \texttt{approach\_service} fragment (corridor affinity $= 0.0$) is not merely down-weighted but entirely excluded.  Similarly, a hospital environment suppresses \texttt{direct\_approach} (affinity $= 0.15 < 0.4$), reflecting the norm against rushing in medical contexts.

The gating threshold operationalizes the theoretical claim from embedded normativity \citep{guenincarlut2023embedded}: the environment does not merely bias behavior---it categorically constrains which behavioral scripts are available.  Below the threshold, a script is not ``unlikely'' but \emph{impossible} in that context.

\subsection{Primitive Library}

The system operates over a library of five registered domain primitives and their derivatives.  Each primitive is an atomic generative model with a single-step $\B$-matrix transition defined by known preconditions and postconditions.  \Cref{tab:primitives} lists the primitives with their social metadata.

\begin{table}[H]
\centering
\small
\begin{tabular}{lllc}
\toprule
Primitive & Preconditions & Postcondition & Deontic \\
\midrule
\texttt{scan-environment} & open\_area, corridor\_encounter & scene\_assessed & permitted \\
\texttt{position-in-queue} & scene\_assessed, open\_area & in\_queue & obligatory \\
\texttt{wait-for-turn} & in\_queue & ready\_for\_service & permitted \\
\texttt{approach-counter} & ready\_for\_service, open\_area & at\_counter & permitted \\
\texttt{engage-staff} & at\_counter, interaction & interaction & permitted \\
\bottomrule
\end{tabular}
\caption{Domain primitives with precondition situations, postcondition situation, and deontic status.  Each primitive is an atomic generative model with a single-step $\B$-matrix transition.}
\label{tab:primitives}
\end{table}

\subsection{Fragment Patterns}

Six fragment patterns serve as partial generative models, each encoding knowledge about a specific aspect of social encounters.  Each fragment contains a cluster of primitives and a $\D$-matrix prior (situation affinity) parameterized across three contexts, as shown in \cref{tab:fragments}.  The rightmost column indicates whether each fragment passes the gating threshold ($\theta = 0.4$) in each context.

\begin{table}[H]
\centering
\small
\begin{tabular}{llcccccc}
\toprule
 & & \multicolumn{3}{c}{$\D$-matrix affinity} & \multicolumn{3}{c}{Active ($\geq 0.4$)} \\
\cmidrule(lr){3-5} \cmidrule(lr){6-8}
Fragment & Primitive cluster & Rec. & Cor. & Hosp. & Rec. & Cor. & Hosp. \\
\midrule
\texttt{observe\_scene} & scan-env., gaze-scan & 0.90 & 0.40 & 0.85 & \checkmark & \checkmark & \checkmark \\
\texttt{queue\_position} & position-in-q., yield-pass & 0.80 & 0.30 & 0.90 & \checkmark & -- & \checkmark \\
\texttt{wait\_patiently} & wait-for-turn, wait-ack. & 0.70 & 0.50 & 0.95 & \checkmark & \checkmark & \checkmark \\
\texttt{approach\_service} & approach-counter, gaze-at-agent & 0.90 & 0.00 & 0.80 & \checkmark & -- & \checkmark \\
\texttt{courtesy\_space} & yield-pass, gaze-avert & 0.30 & 0.80 & 0.60 & -- & \checkmark & \checkmark \\
\texttt{direct\_approach} & approach-counter, engage-staff & 0.95 & 0.20 & 0.15 & \checkmark & -- & -- \\
\bottomrule
\end{tabular}
\caption{Fragment patterns with $\D$-matrix affinities and gating status ($\theta = 0.4$).  Reception activates 5 fragments (service-oriented), corridor activates 3 (passing-oriented), hospital activates 5 (patient queue-oriented).  Note: \texttt{approach\_service} has no corridor affinity key (defaults to 0.0).}
\label{tab:fragments}
\end{table}

The gating pattern reveals how different environments select qualitatively different behavioral repertoires from the same knowledge base.  Reception activates service-oriented fragments including \texttt{direct\_approach} (affinity 0.95) but suppresses \texttt{courtesy\_space} (0.30).  Corridor activates only passing-oriented fragments: \texttt{observe\_scene}, \texttt{wait\_patiently}, and \texttt{courtesy\_space}.  Hospital activates patient queue-oriented fragments, suppressing only \texttt{direct\_approach} (0.15), reflecting the norm against rushing.

\subsection{Composition Pipeline}

The composition pipeline transforms a set of context-gated fragment patterns and a context specification into a composed behavioral sequence through five stages, each corresponding to an operation on the generative model.

The first stage is $\D$-matrix scoring.  For each active fragment $f$ with affinity $a_f(c)$ to context $c$ and precision $p_f$, the posterior relevance is computed as
\begin{equation}
  w_f(c) = a_f(c) \cdot p_f
  \label{eq:d-scoring}
\end{equation}
which is the product of the $\D$-matrix prior and the fragment's confidence.

The second stage is belief propagation via graph diffusion.  With mixing parameter $\alpha$, activation propagates through the pattern graph over multiple iterations.  At each step $t$, the updated weight for fragment $f$ is
\begin{equation}
  w_f^{(t+1)} = (1 - \alpha) \cdot w_f^{(t)} + \alpha \cdot \frac{\displaystyle\sum_{g \in \mathcal{N}(f)} e_{fg} \cdot w_g^{(t)}}{\displaystyle\sum_{g \in \mathcal{N}(f)} e_{fg}}
  \label{eq:diffusion}
\end{equation}
where $\mathcal{N}(f)$ denotes the neighbors of $f$ in the pattern graph and $e_{fg}$ is the edge weight between $f$ and $g$.  Edge weights are computed as $e_{fg} = 0.5 \cdot J(f, g) + 0.5 \cdot \cos(f, g)$, combining the Jaccard similarity of shared primitives with the cosine similarity of affinity profiles.

The third stage is $\B$-matrix construction, in which the \texttt{compose\_from\_patterns} function merges the primitive libraries of the weighted fragments, accumulating primitive weights as $w_p = \sum_{f} w_{p|f} \cdot w_f$.  The merged primitive set then undergoes context topology construction via \cref{eq:b-matrix}, producing a unified $\B$-matrix.

The fourth stage is backbone extraction.  The causal sequence is derived by finding the longest chain of nodes with fan-out equal to 1 in the directed $\B$-matrix graph.  Remaining nodes are inserted based on connectivity strength, while hub nodes are appended at the end.

The fifth and final stage is policy selection via softmax over negative expected free energy (\cref{eq:policy-selection}).  In our experiments, because the pipeline is deterministic and produces a single composed sequence per condition, selection is applied at the primitive level during greedy scoring within backbone extraction.

\subsection{Free Energy of Transitions}

The variational free energy of a single transition under the $\B$-matrix is
\begin{equation}
  \vfe(s_\tau \to s_{\tau+1}) = -\ln \B[s_{\tau+1} | s_\tau, \pi]
  \label{eq:transition-vfe}
\end{equation}
where $\B[s_{\tau+1} | s_\tau, \pi]$ is the normalized weight of the transition from primitive $s_\tau$ to primitive $s_{\tau+1}$ in the context topology, with a floor of $\epsilon = 10^{-6}$ to avoid undefined values.  The total VFE of a composed policy is $\vfe(\pi) = \sum_{\tau=1}^{|\pi|-1} -\ln \B[s_{\tau+1} | s_\tau, \pi_\tau]$.

The expected free energy of the policy incorporates both risk and ambiguity terms following \cref{eq:efe-decomposition}.  Risk measures whether executing the primitive sequence leads to preferred outcomes, while ambiguity measures the entropy of outgoing transition weights from each primitive.


% ================================================================
\section{Experimental Design}
\label{sec:experiments}
% ================================================================

We conduct three computational experiments that systematically vary the generative model's structure and preferences.  These are complete enumerations of the pipeline's response surface under parameter variation, not simulations with stochastic dynamics and not ablation studies over a trained model.  The pipeline is deterministic: given a set of fragments, a context, and the gating threshold, it always produces the same composed sequence.

\subsection{Experiment 1: Material Cue Factorial}

Two base fragments (\texttt{observe\_scene} and \texttt{approach\_service}) are always requested.  Four binary material cues map to optional fragments: stanchions activate \texttt{queue\_position}, a waiting area activates \texttt{wait\_patiently}, a service sign activates \texttt{direct\_approach}, and social density activates \texttt{courtesy\_space}.  The full factorial design yields $2^4 = 16$ cue combinations crossed with 3 contexts for a total of \textbf{48 conditions}.

Critically, the $\D$-matrix gating threshold interacts with context: the same cue may activate a fragment in one context but not another.  For instance, the stanchions cue requests \texttt{queue\_position}, which passes gating in reception (affinity 0.80) and hospital (0.90) but fails in corridor (0.30).  This interaction is the core mechanism by which environment shapes the structural composition of behavior, not just its evaluation.

\subsection{Experiment 2: Fragment Scaling}

All $\binom{6}{k}$ combinations for $k = 1, \ldots, 6$ yield 63 distinct requested fragment sets.  Crossed with 3 contexts, this produces \textbf{189 conditions}.  After $\D$-matrix gating, the number of \emph{active} fragments may be less than $k$, and this reduction is context-dependent: corridor contexts gate more aggressively (only 3 of 6 fragments pass at $k = 6$) than reception or hospital (5 of 6).

\subsection{Experiment 3: Context Transfer}

All 6 fragments are requested in each of 3 contexts, producing \textbf{3 conditions}.  The knowledge base (all primitives, all fragments) is identical across conditions, but $\D$-matrix gating produces different active fragment sets: reception activates 5/6, corridor activates 3/6, hospital activates 5/6.  This is the most direct test of our claim: the same agent ``knows'' the same scripts, but the environment determines which ones are active.

\subsection{Metrics}

For each condition we compute metrics at three levels, as summarized in \cref{tab:metrics}.  With $\D$-matrix gating, all metric levels now show context dependence, though the magnitude varies.

\begin{table}[H]
\centering
\small
\begin{tabular}{llc}
\toprule
Metric & Active inference interpretation & Context effect \\
\midrule
$n_{\text{active}}$ & Fragments passing $\D$-matrix gating & Large ($d \approx 1.0$) \\
$|\pi|$ & Composed policy length & Large ($d \approx 1.0$) \\
Backbone length & $\B$-matrix causal chain length & Large \\
Total VFE & $\sum_\tau -\ln \B[s_{\tau+1}|s_\tau, \pi_\tau]$ (surprisal) & Variable \\
Total EFE & $\sum_\tau \efe(\pi, \tau)$ (expected free energy) & Large ($d \approx 0.6$) \\
$Q(f|c)$ & Posterior fragment weights from belief propagation & Large \\
Jaccard distance & Primitive set divergence across contexts & Large (up to 0.50) \\
$\KL$ from baseline & Policy distribution divergence from reception & Large (up to 2.78) \\
\bottomrule
\end{tabular}
\caption{Metrics computed per condition.  $\D$-matrix gating causes context dependence at every level: structural (which primitives are composed), transitional (VFE of the sequence), and evaluative (EFE of the policy).}
\label{tab:metrics}
\end{table}


% ================================================================
\section{Results}
\label{sec:results}
% ================================================================

\subsection{Experiment 1: Material Cues Shape Behavior}

\Cref{fig:cue-heatmap} shows the mapping from material cues to primitives across the three contexts.  Unlike a system without $\D$-matrix gating, this mapping is \emph{context-dependent}: the same cue activates its associated fragment only if the fragment passes gating in the current context.  In reception, stanchions reliably activate \texttt{position-in-queue}; in corridor, the same stanchions have no effect because \texttt{queue\_position} fails gating (affinity $0.30 < 0.40$).  Chi-square tests confirm that cue presence significantly predicts primitive inclusion differently across contexts ($\chi^2$ values range from 0.0 to 5.33, with corridor showing the most selective gating).

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_exp1_cue_primitive_heatmap.pdf}
  \caption{Material cue to primitive mapping by context.  The same cue produces different primitive activations depending on the $\D$-matrix gating threshold.  Corridor (center) shows the most selective gating: only waiting-area and social-density cues produce effects.}
  \label{fig:cue-heatmap}
\end{figure}

\Cref{fig:n-primitives} demonstrates that context shapes both policy structure and evaluation.  The left panel shows that the number of composed primitives varies across contexts for the same cue configuration: corridor contexts consistently produce shorter policies (2--6 primitives) than reception or hospital (4--9 primitives).  The right panel shows total expected free energy, which also diverges.  The Spearman rank correlation between cue count and policy length is $\rho = 0.53$ for reception, $\rho = 0.85$ for corridor, and $\rho = 0.80$ for hospital, reflecting context-dependent sensitivity to material cue availability.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_exp1_n_primitives_by_cues.pdf}
  \caption{Left: policy length by cue count and context, showing structural divergence.  Right: total EFE by cue count and context.  Both structure and evaluation are context-dependent.}
  \label{fig:n-primitives}
\end{figure}

\Cref{fig:vfe-landscape} shows the expected free energy landscape across cue combinations and contexts.  The three panels reveal qualitatively different landscapes: reception and hospital show similar gradient patterns (both are service contexts), while corridor shows a compressed range with lower absolute values reflecting its sparser active fragment set.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_exp1_free_energy_landscape.pdf}
  \caption{Expected free energy landscape across cue combinations.  Corridor (center) shows a compressed range due to fewer active fragments.  Reception (left) and hospital (right) share similar gradients as service contexts.}
  \label{fig:vfe-landscape}
\end{figure}

\Cref{fig:backbone} shows the distribution of EFE values, and \cref{fig:interaction} shows the context-by-complexity interaction.  The interaction is now driven by both structural and evaluative differences: at zero cues, corridor has only 2 primitives versus 4 for reception, and as cues are added, the structural gap widens because each additional cue is more likely to be gated out in corridor.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/fig_exp1_backbone_by_context.pdf}
  \caption{EFE distribution by cue count.  Corridor occupies a distinct region of the EFE landscape, reflecting both fewer primitives and different preference alignment.}
  \label{fig:backbone}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/fig_exp1_anova_interaction.pdf}
  \caption{Context by cue count interaction.  Non-parallel lines reflect that context modulates how material cues translate into both policy structure and expected free energy.}
  \label{fig:interaction}
\end{figure}


\subsection{Experiment 2: Graceful Degradation}

\Cref{fig:degradation} presents the degradation curves as a dual-panel comparison.  The left panel shows that composed policy length now diverges across contexts at every fragment count: corridor consistently produces shorter policies because $\D$-matrix gating reduces the effective number of active fragments.  The Spearman correlation between requested fragment count and policy length is $\rho = 0.86$ for reception, $\rho = 0.75$ for corridor, and $\rho = 0.87$ for hospital.  The lower corridor correlation reflects the ceiling effect of gating: many requested fragments fail the threshold, so increasing $k$ has diminishing returns.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_exp2_degradation_curves.pdf}
  \caption{Graceful degradation under model sparsity.  Left: policy length by fragment count, now context-dependent due to $\D$-matrix gating.  Right: total EFE by fragment count.  Both panels show corridor (orange) as the most constrained context.}
  \label{fig:degradation}
\end{figure}

\Cref{fig:subset} shows Shannon entropy of posterior fragment weights as a function of fragment count.  Weight distributions remain approximately uniform for active fragments but show context-dependent patterns at higher $k$, reflecting the differential gating across contexts.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/fig_exp2_subset_violations.pdf}
  \caption{Fragment weight entropy vs.\ model richness.  Gray markers indicate maximum possible entropy.}
  \label{fig:subset}
\end{figure}

\Cref{fig:vfe-fragments} shows mean total EFE as a function of fragment count.  The context separation is now driven by both the number of active fragments (structural) and the preference alignment of those fragments (evaluative).

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/fig_exp2_free_energy_vs_fragments.pdf}
  \caption{Mean total EFE vs.\ fragment count.  Context preference structure and $\D$-matrix gating jointly create persistent separation.}
  \label{fig:vfe-fragments}
\end{figure}

\Cref{fig:topology} shows total primitive weight budget, confirming that context-dependent weight allocation reflects the $\D$-matrix affinities: reception receives the highest total weight, corridor the lowest.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/fig_exp2_topology_density.pdf}
  \caption{Total primitive weight budget vs.\ fragment count.  Reception receives the highest weight allocation, corridor the lowest.}
  \label{fig:topology}
\end{figure}


\subsection{Experiment 3: Context Transfer}

With all six fragments requested, $\D$-matrix gating produces three qualitatively different behavioral profiles.  \Cref{tab:exp3} shows the composed behaviors.

\begin{table}[H]
\centering
\small
\begin{tabular}{llcl}
\toprule
Context & Active fragments & $|\pi|$ & Composed sequence \\
\midrule
Reception & 5/6 (no courtesy\_space) & 9 & scan $\to$ position $\to$ wait $\to$ gaze-scan $\to$ approach $\to$ \\
 & & & gaze-at $\to$ \textbf{engage-staff} $\to$ yield $\to$ wait-ack. \\
Corridor & 3/6 (observe, wait, courtesy) & 6 & gaze-scan $\to$ yield $\to$ scan $\to$ gaze-avert $\to$ \\
 & & & wait-ack. $\to$ wait-for-turn \\
Hospital & 5/6 (no direct\_approach) & 9 & scan $\to$ position $\to$ wait $\to$ gaze-scan $\to$ approach $\to$ \\
 & & & yield $\to$ wait-ack. $\to$ gaze-at $\to$ gaze-avert \\
\bottomrule
\end{tabular}
\caption{Experiment 3 results.  Same knowledge base, different contexts, different behaviors.  Reception includes \texttt{engage-staff}; corridor omits all service primitives; hospital omits \texttt{engage-staff} (no \texttt{direct\_approach} fragment).}
\label{tab:exp3}
\end{table}

Three behavioral adaptations are immediately apparent.  First, reception is the only context that includes \texttt{engage-staff}, because it is the only context where \texttt{direct\_approach} (which contributes \texttt{engage-staff}) passes gating (affinity 0.95).  Second, corridor produces a qualitatively different and shorter sequence: it omits all service-oriented primitives (\texttt{position-in-queue}, \texttt{approach-counter}, \texttt{gaze-at-agent}, \texttt{engage-staff}) and instead focuses on observation, yielding, and waiting.  Third, hospital resembles reception structurally but places \texttt{yield-pass} and \texttt{gaze-avert} (courtesy primitives) in the sequence while omitting \texttt{engage-staff}, reflecting a more deferential approach.

\Cref{fig:divergence} quantifies the divergence.  The left panel shows Jaccard distances between primitive sets: reception vs.\ corridor is 0.500 (half the primitives differ), reception vs.\ hospital is 0.200, and corridor vs.\ hospital is 0.333.  The middle panel shows cosine distances between weight profiles (0.546 for reception--corridor).  The right panel shows both policy length and total EFE per context.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_exp3_behavioral_divergence.pdf}
  \caption{Behavioral divergence: same knowledge, different environments.  Left: Jaccard distance between primitive sets (0.500 for reception vs.\ corridor).  Center: cosine distance between weight profiles.  Right: policy length (solid) and total EFE (hatched).}
  \label{fig:divergence}
\end{figure}

\Cref{fig:sequences} shows the composed sequences color-coded by source fragment, making the structural differences visually apparent.  The corridor sequence (6 primitives) is substantially shorter than reception or hospital (9 primitives each), and the primitive \emph{content} differs: corridor contains no pink (approach\_service) or yellow (direct\_approach) primitives.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_exp3_sequence_comparison.pdf}
  \caption{Composed sequences across contexts, color-coded by source fragment.  Corridor is qualitatively different: shorter and limited to observation, waiting, and courtesy primitives.}
  \label{fig:sequences}
\end{figure}

The KL divergence of the primitive weight distribution from the reception baseline is 2.785 for corridor and 1.088 for hospital, confirming that corridor diverges most strongly.  The posterior fragment weights $Q(f|c)$ (\cref{fig:weights}) show that filtered-out fragments (e.g., \texttt{direct\_approach} in hospital) have zero weight, while active fragments are redistributed.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/fig_exp3_weight_profiles.pdf}
  \caption{Posterior fragment weights $Q(f|c)$.  Gated fragments (below threshold) show zero weight.  Corridor's radar profile is dramatically smaller and differently shaped.}
  \label{fig:weights}
\end{figure}


\subsection{Cross-Experiment: Context Effect Sizes}

\Cref{fig:effect-sizes} shows Cohen's $d$ effect sizes for key metrics between context pairs.  The central finding is that environment shapes behavior at \emph{every} level.  Structural effects are large: Cohen's $d = 1.02$ for policy length between reception and corridor, driven by $\D$-matrix gating.  Evaluative effects are also large: $d = 0.56$ for EFE between reception and corridor.  The reception--hospital comparison shows near-zero structural effects ($d = -0.02$) because both are service contexts activating similar fragment sets, but the corridor--hospital comparison shows large effects ($d = -1.04$) because corridor is structurally distinct.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_context_effect_sizes.pdf}
  \caption{Cohen's $d$ effect sizes.  $\D$-matrix gating produces large structural effects (up to $d = 1.02$) for comparisons involving corridor.  Dashed lines: small ($d = 0.2$); dotted lines: large ($d = 0.8$).}
  \label{fig:effect-sizes}
\end{figure}


% ================================================================
\section{Discussion}
\label{sec:discussion}
% ================================================================

\subsection{Environment Inscribes Norms (RQ1)}

Our results provide computational evidence for the claim that material environments inscribe behavioral norms into robot cognition.  The $\D$-matrix gating mechanism (\cref{eq:d-gating}) operates as a categorical constraint: fragments below the gating threshold are not merely unlikely but absent from the composition.  This categorical quality aligns with \citet{akrich1992description}'s concept of technical inscription---the environment does not suggest behavior, it specifies which behaviors are available.

The context-dependent cue-primitive mapping (\cref{fig:cue-heatmap}) demonstrates this concretely: the same stanchions that enable queuing in reception have no effect in corridor, because the queuing script is categorically suppressed.  The norm is not merely inscribed in the stanchions---it is inscribed in the \emph{conjunction} of stanchions and context.

\subsection{Compositional Robustness (RQ2)}

Graceful degradation remains an inherent property of the pipeline: sparser generative models yield shorter but viable policies.  However, $\D$-matrix gating reveals a new dimension of this robustness.  The \emph{effective} model sparsity is context-dependent: corridor contexts experience more aggressive gating, so their degradation curves lie below those of reception and hospital (\cref{fig:degradation}).  The pipeline adapts to this context-dependent sparsity, producing viable corridor behaviors (yielding, waiting, courtesy) from as few as 2 active fragments.

The Spearman correlations ($\rho > 0.74$ for all contexts) confirm monotonic relationships between model richness and behavioral complexity, but the slope differs: corridor shows a lower correlation ($\rho = 0.75$) because gating creates a ceiling effect.  This is a desirable property---it means the robot's behavioral repertoire in constrained environments (corridors) is robust to the addition of irrelevant knowledge.

\subsection{Environment Shapes Behavior, Not Just Evaluation (RQ3)}

Experiment 3 demonstrates the most striking finding: the same knowledge base yields qualitatively different behaviors across contexts.  The Jaccard distance of 0.50 between reception and corridor primitive sets means that half the primitives differ---this is not a subtle weight redistribution but a categorical behavioral change.  The corridor robot does not queue, does not approach a counter, does not engage staff.  It observes, yields, and waits.  The hospital robot queues patiently but does not engage staff directly.  The reception robot does everything including direct staff engagement.

This result realizes \citeauthor{lefebvre1991production}'s \citeyearpar{lefebvre1991production} thesis computationally: change the space, change the cognition.  The mechanism is precise: the $\D$-matrix gating threshold (\cref{eq:d-gating}) translates environmental context into a categorical constraint on which generative sub-models participate in composition.  The large Cohen's $d$ values ($d \approx 1.0$ for structural comparisons involving corridor) confirm that these are not marginal perturbations but fundamental behavioral reorganizations.

\subsection{Convergence with Thinking Machines}

Our composition pipeline mirrors the dual-loop architecture proposed by \citet{nair2026thinking}.  The fragment graph implements the slow compositional memory; graph diffusion (\cref{eq:diffusion}) implements belief propagation on this factor graph.  The $\D$-matrix gating adds a new mechanism not present in the original proposal: contextual pre-filtering of the memory contents, ensuring that only contextually relevant fragments enter the composition process.  This aligns with the ecological notion that memory retrieval is always situated---what you can remember depends on where you are.

\subsection{Active Inference as Unifying Framework}

All components of the pipeline are instances of the same free energy minimization principle operating at different scales.  $\D$-matrix gating (\cref{eq:d-gating}) minimizes surprise by excluding fragments that would generate high-surprise predictions in the current context.  Fragment scoring (\cref{eq:d-scoring}) weights remaining fragments by their posterior relevance.  Graph diffusion (\cref{eq:diffusion}) propagates these weights through the factor graph.  Topology construction (\cref{eq:b-matrix}) builds context-dependent transitions.  Policy evaluation minimizes expected free energy, balancing risk and ambiguity.

\subsection{Limitations}

Several limitations should be acknowledged.  The gating threshold $\theta = 0.4$ is manually specified; a principled Bayesian approach would learn $\theta$ from data as a precision parameter.  The $\D$-matrix affinities are manually specified rather than learned from experience.  The pipeline selects policies deterministically via argmax rather than sampling from $P(\pi)$.  Our discrete state space is a simplification of real environments.  The pipeline composes a sequence once rather than adapting online.  Finally, because our experiments enumerate a deterministic pipeline, the statistical tests reported here are descriptive characterizations of the complete response surface rather than inferential tests over a sample.


% ================================================================
\section{Conclusion}
\label{sec:conclusion}
% ================================================================

We have presented computational experiments demonstrating that material environment structure shapes robot social cognition at every level---structural, transitional, and evaluative.  Through 240 experimental conditions across three experiments, we show that material cues interact with environmental context to determine which behavioral fragments are activated ($\rho > 0.53$ for cue--policy correlation across all contexts), that the compositional pipeline degrades gracefully under context-dependent model sparsity ($\rho > 0.74$), and that identical knowledge bases yield qualitatively different behaviors under different contexts (Jaccard distance up to 0.50, Cohen's $d = 1.02$ for structural effects).

The $\D$-matrix gating mechanism is the key architectural contribution: by thresholding fragment affinities against the current context, the pipeline translates environmental constraints into categorical behavioral differences.  A corridor robot composes 6 primitives focused on yielding and courtesy.  A reception robot composes 9 primitives including queuing and staff engagement.  A hospital robot composes 9 primitives with patient queuing but without direct staff engagement.  These are not different evaluations of the same behavior---they are different behaviors entirely.

The environment shapes cognition.  The norm was in the stanchions.


% ================================================================
\bibliographystyle{apalike}
\bibliography{references}

\end{document}
